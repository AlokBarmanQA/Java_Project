package arrays.manipulation.algorithms;

public class SearchAnElementInAnArrayUsingLinearSearch {

	public static void main(String[] args) {
		/*
		 * It accepts an integer array (arr) and a target integer (target) as input
		 * parameters. A for loop iterates through the array from the first element
		 * (index 0) to the last element (arr.length - 1). In each iteration, it
		 * compares the current array element (arr[i]) with the target value. If a match
		 * is found, the method immediately returns the current index i. If the loop
		 * finishes without finding a match after checking all elements, it returns -1
		 * to indicate the element is not present.
		 */

	}

}
